/* exported drop */
// use slice method with argument - count number as start point
// return the result in new array

function drop(array, count) {
  var newArr = array.slice(count);
  return newArr;

}
